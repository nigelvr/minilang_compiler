[a-zA-Z_][a-zA-Z_0-9]* {
    auto it = Environment.find(yytext);
    if (it == Envrionment.end()) {
        yyerror("Could not find %s in environemnt\n", yytext);
    }
    yylval.d = atoi(it->second());
    return IDENT;
}